"""Optimization sub-system for distributed DNN inference.

Provides channel estimators, accuracy surrogate models, compression mappers,
and slot-level optimizer implementations (no-CSI, CSI-aware, random, FIFO).
"""
